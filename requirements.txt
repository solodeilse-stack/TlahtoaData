# Tlahtoa Data — runtime dependencies
# Aligned with pyproject.toml [project.dependencies] + deploy
pandas>=2.0
pyyaml>=6.0
pydantic>=2.0
jsonschema>=4.0
openpyxl>=3.1
tabulate>=0.9
pydataset>=0.2

# Dev / test
pytest>=7.0
plotly>=5.0

# Simulator API (Railway)
fastapi>=0.110
uvicorn[standard]>=0.29

# Ingesta ETJ CONAGUA (Fase B — scripts/ingesta)
requests>=2.28
backoff>=2.2
pdfplumber>=0.11

# Ingesta CONAGUA HTML (scripts/ingesta/conagua_scraper)
beautifulsoup4>=4.12
lxml>=5.0
